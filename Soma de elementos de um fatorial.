#include <stdio.h>

int main()
{
int fat, n;
printf("Insira um valor para o qual deseja calcular a soma de todos os inteiros entre 0 e o valor desejado:  ");
scanf("%d", &n);

for(fat = 1; n > 1; n = n - 1)
fat = fat + n;

printf("\nA soma vale: %d", fat);
return 0;
}
